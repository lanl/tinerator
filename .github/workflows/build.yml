name: Python Package using Conda - new

on:
  push:
    branches:
      - "rich/regroup"

jobs:
  build-matrix:
    name: build (${{ matrix.os }}, ${{ matrix.python-version }})
    runs-on: ${{ matrix.os }}-latest
    strategy:
      fail-fast: false
      matrix:
        os: ["ubuntu"]
        python-version: ["3.11", "3.10", "3.9", "3.8"]

    steps:
    - uses: actions/checkout@v4
    - uses: conda-incubator/setup-miniconda@v2
      with: 
        activate-environment: tinerator-v1
        channels: conda-forge,defaults
        channel-priority: strict
        environment-file: environment.yml
        python-version: ${{ matrix.python-version }}
        auto-activate-base: false
    - name: Build TPLs
      run: |
        ./tpls/build-tpls.sh -je
        echo "PYTHONPATH=/home/runner/work/tinerator/tinerator/tpls/seacas/install/lib/:${PYTHONPATH}" >> $GITHUB_ENV
    - name: Build TINerator
      run: |
        python -m pip install . -vv
    #- name: Lint with flake8
    #  run: |
    #    conda install flake8
    #    # stop the build if there are Python syntax errors or undefined names
    #    flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
    #    # exit-zero treats all errors as warnings. The GitHub editor is 127 chars wide
    #    flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
    - name: Test with pytest
      run: |
       cd tests; $CONDA/envs/tinerator-v1/bin/pytest
