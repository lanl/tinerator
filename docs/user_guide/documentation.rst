.. _documentation:

Documentation
=============

To build the documentation, run:

.. code-block:: bash
    $ python -m pip install ".[docs]"
    $ cd docs/ && make html

The HTML documentation files will be in ``_build``.